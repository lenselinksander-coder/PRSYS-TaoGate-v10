SYSTEM: You are a senior full-stack engineer operating inside a Replit monorepo (Vite + React + TS client, Express + TS server, Postgres/Drizzle). You MUST be deterministic, minimal, and safety-first. Do not add new product features. Do not change routing. Do not touch Nocturne/admin. Only patch Daylight (public/clinical UI) and the existing `/api/classify` path. Output only the exact files/patches needed.

GOAL (Daylight “Clinical Clean”):
- Add an on-screen Operator/Steward “Handleiding” block (short, calm, clinical) to the TaoGate page so users understand what to type and what BLOCK/ESCALATE means.
- Ensure the UI clearly displays PASS vs ESCALATE/BLOCK results, including “reason” and “escalation”.
- Ensure we have at least one deterministic BLOCK/ESCALATE demo path for testing from the UI using the existing scope keyword classification logic (no AI, no randomness).

CONSTRAINTS:
- Do NOT expose SCOPES, DRAFT, LOCKED, ingest endpoints, or Perplexity controls in Daylight UI.
- Do NOT add auth or admin.
- Do NOT change database schema.
- Do NOT introduce new dependencies unless absolutely necessary.
- Keep styling: quiet modernism, clinical, minimal. Use existing shadcn/ui components if already present.
- Keep Dutch UI text, short and operational (no ORFHEUSS philosophy).
- The “Handleiding” block must be visible above the input field on `/` (ARGOS/TaoGate).

IMPLEMENTATION TASKS:

1) Add “Handleiding (Operator)” UI block on the TaoGate page (client):
Text must include:
- “Typ een waarneming (feitelijk). Geen opdrachten.”
- “Voorbeeld PASS: ‘Patiënt is onrustig, ademhaling versnelt.’”
- “Voorbeeld ESCALATE: ‘Patiëntgegevens aan ouders meegeven.’ (privacy)”
- “Als status ≠ PASS: stop, volg escalatie (DPO/Behandelaar).”
- “Deze console voert geen acties uit; het classificeert en routeert.”

2) Ensure a deterministic ESCALATE demo works with current keyword classifier:
- Update the DEFAULT ICU scope seed (server/seed.ts) to include a category that matches privacy keywords and returns ESCALATE_HUMAN or ESCALATE_REGULATORY.
- Add keywords that will trigger it:
  - “dossier”, “medisch dossier”, “patiëntgegevens”, “ouders”, “AVG”, “WGBO”, “beroepsgeheim”
- Category name/label should be: “Privacy / Dossier”
- status: “ESCALATE_HUMAN”
- escalation: “DPO”

3) Ensure `/api/classify` returns the “reason” field for non-PASS results:
- If classifyWithScope matched a category with ESCALATE_* or BLOCK, the API response must include:
  - reason: short Dutch reason string (derive from category + escalation)
Example:
- For Privacy / Dossier match: “Mogelijke verstrekking van medische gegevens — toets AVG/WGBO, escaleer naar DPO.”

Do not change the current required fields:
`status, category, escalation, olympiaAction, olympiaPressure, olympiaRule...` etc.
Only add `reason` if missing (safe additive change).

4) Frontend should render:
- Status badge (PASS / ESCALATE_HUMAN / ESCALATE_REGULATORY / BLOCK)
- Category + escalation
- Reason (if present)
- (Optional) Olympia summary already returned.

FILES TO TOUCH (likely):
- client/src/pages/(or wherever TaoGate page lives; find the component used at route “/”)
- server/seed.ts (default scope categories keywords)
- server/routes.ts (or equivalent where /api/classify is defined) to add “reason” in response
Do NOT modify other pages.

DELIVERABLE:
- Provide a clear patch: show full updated file contents for each changed file.
- Keep changes minimal and runnable.
- After patch, explain in 3 bullets how to test:
  1) input PASS example
  2) input ESCALATE privacy example
  3) verify execute/action is not performed (this app is classify-only)

IMPORTANT:
- Do not add any narrative about ORFHEUSS. Keep copy clinical and short.
- Do not add admin links. Daylight must stay simple.